# inventory-cmdb

This repository contains an Ansible inventory file (`inventory_cmdb.yaml`) for managing hosts.

## Usage

To view the inventory using Ansible:

```bash
$ ansible-inventory -i inventory_cmdb.yaml --list
```
